/// Maximum length of player display name
pub const MAX_NAME_LENGTH: usize = 32;

/// Initial level
pub const INITIAL_LEVEL: u8 = 0;

/// Initial total runs
pub const INITIAL_TOTAL_RUNS: u32 = 0;

/// Initial available runs
pub const INITIAL_AVAILABLE_RUNS: u32 = 40;
